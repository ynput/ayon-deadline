{
  "files": [
    {
      "path": "server_addon/deadline/server/settings/collect_job_info.py",
      "content": "import pyblish.api\nfrom ayon_server.settings import BaseSettingsModel, SettingsField\n\n\nclass CollectJobInfoModel(BaseSettingsModel):\n    _layout = \"expanded\"\n\n    job_type: str = SettingsField(\n        \"all\",\n        title=\"Job Type\",\n        enum=[\n            (\"all\", \"All\"),\n            (\"render\", \"Render\"),\n            (\"publish\", \"Publish\"),\n            (\"cache\", \"Cache\"),\n        ],\n        description=\"Apply overrides only to jobs of this type. 'All' applies to every job.\"\n    )\n    attributes: dict = SettingsField(\n        default={},\n        title=\"Attributes\",\n        description=\"Override Deadline job attributes (e.g., Priority, Pool, etc.)\"\n    )\n    # Other fields as needed (environment, etc.)\n\n\nclass CollectJobInfoSettings(BaseSettingsModel):\n    """Settings for CollectJobInfo plugin."""\n    enabled: bool = SettingsField(True, title=\"Enabled\")\n    profiles: list[CollectJobInfoModel] = SettingsField(\n        default=[],\n        title=\"Profiles\"\n    )\n"
    },
    {
      "path": "client/ayon_deadline/plugins/publish/collect_job_info.py",
      "content": "import pyblish.api\nfrom ayon_deadline import lib as deadline_lib\nfrom ayon_deadline.lib import DeadlineJobType\n\n\nclass CollectJobInfo(pyblish.api.InstancePlugin):\n    \"\"\"Apply Deadline job overrides based on profile matching.\"\"\"\n\n    order = pyblish.api.CollectorOrder - 0.499\n    label = \"Collect Job Info\"\n    families = [\"render\", \"publish\", \"cache\"]  # All job families\n\n    def process(self, instance):\n        job_type = instance.data.get(\"deadlineJobType\", \"\") or \\\n            instance.data.get(\"family\", \"\")\n        # Map family to job type, fallback to 'render'\n        if job_type in (\"render\", \"publish\", \"cache\"):\n            pass\n        else:\n            job_type = \"render\"\n\n        profiles = self.get_profiles(instance)\n        matched_profile = self.match_profile(instance, profiles, job_type)\n        if matched_profile:\n            self.apply_overrides(instance, matched_profile, job_type)\n\n    def get_profiles(self, instance):\n        # retrieve profiles from settings via context\n        return instance.context.data.get(\"deadline_collect_job_info_profiles\", [])\n\n    def match_profile(self, instance, profiles, job_type):\n        for profile in profiles:\n            # Check job_type filter\n            profile_job_type = profile.get(\"job_type\", \"all\")\n            if profile_job_type != \"all\" and profile_job_type != job_type:\n                continue\n            # Add other matching logic (e.g., task type, host, etc.)\n            # For now, return first matching profile\n            return profile\n        return None\n\n    def apply_overrides(self, instance, profile, job_type):\n        attributes = profile.get(\"attributes\", {})\n        # Prefix keys with job type to avoid conflicts in stored overrides\n        prefix = f\"{job_type}_\" if job_type else \"\"\n        for key, value in attributes.items():\n            # Override instance data with prefix\n            instance.data[f\"deadline_{prefix}{key}\"] = value\n"
    }
  ],
  "summary": "Modified CollectJobInfo settings to include a 'job_type' enum field for filtering profiles by job type (all, render, publish, cache). Updated the client-side CollectJobInfo plugin to match profiles based on job type and prefix overridden attributes with job type to avoid conflicts in stored overrides. This enables using CollectJobInfo for all job types, addressing the requirement to differentiate between render, publish, and cache jobs."
}